@using ProjectSafeHostel.Dominio.Entities;
@using ProjectSafeHostel.Servico.ViewModels;

@{
    ViewData["Title"] = "Funcionario";
}

@model PaginacaoViewModel<Colaborador>

<br />

<a class="btn btn-xs bg-green-500 text-white" asp-controller="Funcionario" asp-action="CadastrarFuncionario">
    Novo Funcionário
</a>

<br />
<br />
<br />

<table class="table table-striped table-sm table-responsive-md">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nome Funcionário</th>
            <th>Data de Nascimento</th>
            <th>CPF</th>
            <th>Excluir</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Itens)
        {
            <tr>
                <td>
                    @item.COLABORADOR_ID
                </td>
                <td>
                    @item.NOME  @item.SOBRENOME
                </td>
                <td>
                    @item.DATA_NASCIMENTO
                </td>
                <td>
                    @item.CPF.ToString()
                </td>
                <td>
                    <a class="btn btn-danger btn-xs text-white" asp-action="Excluir" asp-controller="Funcionario" asp-route-id="@item.COLABORADOR_ID">
                        X
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>